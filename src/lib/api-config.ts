// ==========================================
// Global Backend API Configuration
// ==========================================
// Change this URL when switching backend environments.
// Override at runtime via NEXT_PUBLIC_API_URL in .env

export const API_BASE =
  process.env.NEXT_PUBLIC_API_URL;
